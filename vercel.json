{
  "version": 2,
  "builds": [
    {
      "src": "portal/next.config.js",
      "use": "@vercel/next",
      "config": { "distDir": ".next" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/portal/$1" }
  ]
}
